import 'package:flutter/material.dart';

class VerificationMapScreen extends StatelessWidget {
  const VerificationMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Verification Map Screen'),
      ),
    );
  }
}
